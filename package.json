{
  "scripts": {
    "build": "npm run build:leaner && npm run build:duplicates",
    "build:leaner": "npx tailwindcss -i ./src/leaner.css -o ./leaner.css --minify",
    "build:duplicates": "npx tailwindcss -i ./src/duplicates.css -o ./duplicates.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.7",
    "tailwindcss": "^3.1.8"
  }
}